diff --git a/dist/chat-DOMgZOP2.js b/dist/chat-DOMgZOP2.js
index 2e65c24..d6cd269 100644
--- a/dist/chat-DOMgZOP2.js
+++ b/dist/chat-DOMgZOP2.js
@@ -2683,6 +2683,10 @@ const chatHandlers = {
 						imageOrder: imageOrder.length > 0 ? imageOrder : void 0,
 						thinkingLevelOverride: p.thinking,
 						fastModeOverride: p.fastMode,
+						extraSystemPrompt: p.extraSystemPrompt,
+						executeAsAgentId: typeof p.executeAsAgentId === "string" && p.executeAsAgentId.trim() ? p.executeAsAgentId.trim() : void 0,
+						executedByAgentName: typeof p.executedByAgentName === "string" && p.executedByAgentName.trim() ? p.executedByAgentName.trim() : void 0,
+						skillFilter: Array.isArray(p.skillFilter) ? p.skillFilter.map((entry) => typeof entry === "string" ? entry.trim() : "").filter(Boolean) : void 0,
 						userTurnTranscriptRecorder: userTurnRecorder,
 						onAgentRunStart: (runId) => {
 							agentRunStarted = true;
diff --git a/dist/compaction-successor-transcript-DOYIvTa2.js b/dist/compaction-successor-transcript-DOYIvTa2.js
index dc91ddb..ea03db0 100644
--- a/dist/compaction-successor-transcript-DOYIvTa2.js
+++ b/dist/compaction-successor-transcript-DOYIvTa2.js
@@ -39,7 +39,7 @@ import { i as streamSimple, o as getApiProvider } from "./stream-CGV1FC9I.js";
 import { r as createAssistantMessageEventStream } from "./event-stream-ReMmOTzX.js";
 import { at as SettingsManager, s as DefaultResourceLoader } from "./sessions-D4hg9w_o.js";
 import { n as SUMMARIZATION_OVERHEAD_TOKENS } from "./compaction-planning-C60ze-Y9.js";
-import { a as isSilentReplyText, r as isSilentReplyPayloadText } from "./tokens-Doqt0ySJ.js";
+import { a as isSilentReplyText, r as isSilentReplyPayloadText, c as stripSilentToken, s as stripLeadingSilentToken, o as startsWithSilentToken, n as SILENT_REPLY_TOKEN } from "./tokens-Doqt0ySJ.js";
 import { b as stripInternalMetadataForDisplay } from "./session-utils.fs-C-Xn7QJC.js";
 import { a as makeZeroUsageSnapshot, o as normalizeUsage } from "./usage-C67Kbb7n.js";
 import { i as jsonUtf8BytesOrInfinity, n as firstEnumerableOwnKeys, t as boundedJsonUtf8Bytes } from "./json-utf8-bytes-C14lActR.js";
@@ -1525,6 +1525,64 @@ function isTranscriptOnlyOpenClawAssistantMessage(message) {
 	if (!message || message.role !== "assistant") return false;
 	return isTranscriptOnlyOpenClawAssistantModel(normalizeOptionalString(message.provider) ?? "", normalizeOptionalString(message.model) ?? "");
 }
+function sanitizeAssistantReplayVisibleText(text, silentToken = SILENT_REPLY_TOKEN) {
+	let next = text;
+	if (!next) return "";
+	const trimmedInitial = next.trim();
+	if (!trimmedInitial || isSilentReplyPayloadText(trimmedInitial, silentToken)) return "";
+	if (!isSilentReplyText(next, silentToken)) {
+		const hadLeadingSilentToken = startsWithSilentToken(next, silentToken);
+		if (hadLeadingSilentToken) next = stripLeadingSilentToken(next, silentToken);
+		if (hadLeadingSilentToken || next.toLowerCase().includes(silentToken.toLowerCase())) next = stripSilentToken(next, silentToken);
+	}
+	const trimmed = next.trim();
+	if (!trimmed || isSilentReplyPayloadText(trimmed, silentToken)) return "";
+	return next;
+}
+function sanitizeAssistantMessageForTranscriptPersistence(message) {
+	if (!message || message.role !== "assistant") return message;
+	const content = message.content;
+	if (typeof content === "string") {
+		const sanitized = sanitizeAssistantReplayVisibleText(stripInternalMetadataForDisplay(content));
+		if (!sanitized.trim()) return null;
+		if (sanitized === content) return message;
+		return {
+			...message,
+			content: sanitized
+		};
+	}
+	if (!Array.isArray(content)) return message;
+	let touched = false;
+	const nextContent = [];
+	for (const block of content) {
+		if (!block || typeof block !== "object") {
+			nextContent.push(block);
+			continue;
+		}
+		const text = block.text;
+		if (typeof text !== "string") {
+			nextContent.push(block);
+			continue;
+		}
+		const sanitized = sanitizeAssistantReplayVisibleText(stripInternalMetadataForDisplay(text));
+		if (sanitized === text) {
+			if (sanitized.trim()) nextContent.push(block);
+			else touched = true;
+			continue;
+		}
+		touched = true;
+		if (sanitized.trim()) nextContent.push({
+			...block,
+			text: sanitized
+		});
+	}
+	if (!touched) return message;
+	if (nextContent.length === 0) return null;
+	return {
+		...message,
+		content: nextContent
+	};
+}
 function installSessionToolResultGuard(sessionManager, opts) {
 	const originalAppend = getRawSessionAppendMessage(sessionManager);
 	setRawSessionAppendMessage(sessionManager, originalAppend);
@@ -1627,8 +1685,13 @@ function installSessionToolResultGuard(sessionManager, opts) {
 		const toolCalls = nextRole === "assistant" && stopReason !== "aborted" && stopReason !== "error" ? extractToolCallsFromAssistant(nextMessage) : [];
 		if (!(nextRole === "assistant" && toolCalls.length === 0 && isTranscriptOnlyOpenClawAssistantMessage(nextMessage)) && pendingState.shouldFlushBeforeNonToolResult(nextRole, toolCalls.length)) flushPendingToolResults();
 		if (!allowSyntheticToolResults && pendingState.shouldFlushBeforeNewToolCalls(toolCalls.length)) flushPendingToolResults();
-		const finalMessage = applyBeforeWriteHook(persistMessage(nextMessage));
+		let finalMessage = applyBeforeWriteHook(persistMessage(nextMessage));
 		if (!finalMessage) return;
+		if (finalMessage.role === "assistant") {
+			const sanitizedAssistant = sanitizeAssistantMessageForTranscriptPersistence(finalMessage);
+			if (!sanitizedAssistant) return;
+			finalMessage = sanitizedAssistant;
+		}
 		const finalRole = finalMessage.role;
 		if (finalRole === "assistant" && toolCalls.length === 0 && opts?.suppressTranscriptOnlyAssistantPersistence === true) return;
 		if (finalRole === "assistant" && opts?.suppressAssistantErrorPersistence === true && finalMessage.stopReason === "error") return;
@@ -3676,9 +3739,9 @@ function sanitizeUserReplayContent(message) {
 	} : message;
 }
 function normalizeAssistantReplayTextContent(message, replayContent) {
-	const strippedText = stripInternalMetadataForDisplay(replayContent);
+	const strippedText = sanitizeAssistantReplayVisibleText(stripInternalMetadataForDisplay(replayContent));
 	const trimmed = strippedText.trim();
-	if (!trimmed || isSilentReplyPayloadText(trimmed, "NO_REPLY")) return null;
+	if (!trimmed) return null;
 	return {
 		...message,
 		content: [{
@@ -3700,15 +3763,14 @@ function normalizeAssistantReplayBlockContent(message, replayContent) {
 			sanitizedContent.push(block);
 			continue;
 		}
-		const strippedText = stripInternalMetadataForDisplay(text);
+		const strippedText = sanitizeAssistantReplayVisibleText(stripInternalMetadataForDisplay(text));
 		if (strippedText === text) {
-			if (!isSilentReplyPayloadText(text.trim(), "NO_REPLY")) sanitizedContent.push(block);
+			if (strippedText.trim()) sanitizedContent.push(block);
 			else touched = true;
 			continue;
 		}
 		touched = true;
-		const trimmed = strippedText.trim();
-		if (trimmed && !isSilentReplyPayloadText(trimmed, "NO_REPLY")) sanitizedContent.push({
+		if (strippedText.trim()) sanitizedContent.push({
 			...block,
 			text: strippedText
 		});
diff --git a/dist/context-window-guard-BEhHCr1H.js b/dist/context-window-guard-BEhHCr1H.js
index 5432832..35ec5be 100644
--- a/dist/context-window-guard-BEhHCr1H.js
+++ b/dist/context-window-guard-BEhHCr1H.js
@@ -35,9 +35,14 @@ function resolveContextWindowInfo(params) {
 			provider: params.provider,
 			modelId: params.modelId
 		}));
-		return normalizePositiveInt(match?.contextTokens) ?? normalizePositiveInt(match?.contextWindow);
+		const fromConfigTokens = normalizePositiveInt(match?.contextTokens);
+		const fromConfigWindow = normalizePositiveInt(match?.contextWindow);
+		if (fromConfigTokens !== void 0 && fromConfigWindow !== void 0) return Math.max(fromConfigTokens, fromConfigWindow);
+		return fromConfigTokens ?? fromConfigWindow;
 	})();
-	const fromModel = normalizePositiveInt(params.modelContextTokens) ?? normalizePositiveInt(params.modelContextWindow);
+	const fromModelTokens = normalizePositiveInt(params.modelContextTokens);
+	const fromModelWindow = normalizePositiveInt(params.modelContextWindow);
+	const fromModel = fromModelTokens !== void 0 && fromModelWindow !== void 0 ? Math.max(fromModelTokens, fromModelWindow) : fromModelTokens ?? fromModelWindow;
 	const defaultTokens = normalizePositiveInt(params.defaultTokens) ?? 8e3;
 	const baseInfo = fromModelsConfig ? {
 		tokens: fromModelsConfig,
diff --git a/dist/embedded-agent-CpxCz9I4.js b/dist/embedded-agent-CpxCz9I4.js
index a611909..e2a6081 100644
--- a/dist/embedded-agent-CpxCz9I4.js
+++ b/dist/embedded-agent-CpxCz9I4.js
@@ -1874,10 +1874,11 @@ function resolveEffectiveRuntimeModel(params) {
 		modelContextWindow: params.runtimeModel.contextWindow,
 		defaultTokens: DEFAULT_CONTEXT_TOKENS
 	});
-	const effectiveModel = ctxInfo.tokens < (params.runtimeModel.contextWindow ?? Infinity) ? {
+	const effectiveModel = {
 		...params.runtimeModel,
-		contextWindow: ctxInfo.tokens
-	} : params.runtimeModel;
+		...(ctxInfo.tokens < (params.runtimeModel.contextWindow ?? Infinity) ? { contextWindow: ctxInfo.tokens } : {}),
+		contextTokens: ctxInfo.tokens
+	};
 	const ctxGuard = evaluateContextWindowGuard({ info: ctxInfo });
 	const runtimeBaseUrl = typeof params.runtimeModel.baseUrl === "string" ? params.runtimeModel.baseUrl : void 0;
 	if (ctxGuard.shouldWarn) log$1.warn(formatContextWindowWarningMessage({
diff --git a/dist/get-reply-BpFiu3Nn.js b/dist/get-reply-BpFiu3Nn.js
index 97733b6..76c321a 100644
--- a/dist/get-reply-BpFiu3Nn.js
+++ b/dist/get-reply-BpFiu3Nn.js
@@ -133,6 +133,7 @@ import fs from "node:fs";
 import path from "node:path";
 import fs$1 from "node:fs/promises";
 import crypto from "node:crypto";
+import { homedir as userHomeDir } from "node:os";
 //#region src/auto-reply/reply/get-reply-directives-utils.ts
 const CLEARED_EXEC_FIELDS = {
 	hasExecDirective: false,
@@ -5046,13 +5047,122 @@ async function applyLinkUnderstandingIfNeeded(params) {
 		return false;
 	}
 }
+async function pathExistsSafe(filePath) {
+	try {
+		await fs$1.access(filePath);
+		return true;
+	} catch {
+		return false;
+	}
+}
+async function readJsonFileSafe(filePath) {
+	try {
+		return JSON.parse(await fs$1.readFile(filePath, "utf-8"));
+	} catch {
+		return null;
+	}
+}
+async function resolveDigitalEmployeeDir(agentId) {
+	const home = userHomeDir();
+	const legacyDir = path.join(home, ".openclaw", "digital-employee", agentId);
+	if (await pathExistsSafe(legacyDir)) return legacyDir;
+	const managedRoot = path.join(home, ".openclaw", "digital-employees");
+	const directManagedDir = path.join(managedRoot, agentId);
+	if (await pathExistsSafe(directManagedDir)) return directManagedDir;
+	if (!await pathExistsSafe(managedRoot)) return null;
+	try {
+		for (const entry of await fs$1.readdir(managedRoot, { withFileTypes: true })) {
+			if (!entry.isDirectory()) continue;
+			const installDir = path.join(managedRoot, entry.name);
+			const record = await readJsonFileSafe(path.join(installDir, "install.json"));
+			if (record?.agentId === agentId || record?.instanceId === agentId) return installDir;
+		}
+	} catch {}
+	return null;
+}
+function isPortableDigitalEmployeeEntryPath(value) {
+	if (typeof value !== "string" || !value.trim()) return false;
+	const trimmed = value.trim();
+	if (path.isAbsolute(trimmed) || /^[A-Za-z]:[\\/]/.test(trimmed) || trimmed.startsWith("~")) return false;
+	const normalized = trimmed.replace(/\\/g, "/");
+	return normalized !== ".." && !normalized.startsWith("../") && !normalized.includes("/../");
+}
+function expandDigitalEmployeeRuntimeMcpServer(entry, employeeDir) {
+	if (entry?.runtime !== "node") return { ...entry };
+	if (!isPortableDigitalEmployeeEntryPath(entry.entry)) throw new Error("Digital employee MCP runtime entry must be a package-relative path");
+	if (entry.command !== void 0) throw new Error("Digital employee MCP runtime entry must not also set command");
+	const { runtime: _runtime, entry: entryPath, args, env, ...rest } = entry;
+	const nodeCommand = process.env.CLAWX_NODE || process.execPath;
+	return {
+		...rest,
+		command: nodeCommand,
+		args: [path.resolve(employeeDir, entryPath), ...(Array.isArray(args) ? args : [])],
+		env: { ...env, CLAWX_NODE: nodeCommand, EMPLOYEE_DIR: employeeDir },
+	};
+}
+async function buildDigitalEmployeeMcpServers(employeeDir) {
+	const manifest = await readJsonFileSafe(path.join(employeeDir, "employee.json"));
+	const templatePath = typeof manifest?.mcp?.serverTemplate === "string" && manifest.mcp.serverTemplate.trim()
+		? path.join(employeeDir, manifest.mcp.serverTemplate)
+		: path.join(employeeDir, "mcp", "servers.json");
+	let config = await readJsonFileSafe(templatePath);
+	if (!config && templatePath.endsWith("servers.json")) config = await readJsonFileSafe(path.join(employeeDir, "mcp", "servers.template.json"));
+	const sourceServers = config?.servers && typeof config.servers === "object" ? config.servers : {};
+	return Object.fromEntries(Object.entries(sourceServers).map(([name, entry]) => {
+		const next = expandDigitalEmployeeRuntimeMcpServer(entry, employeeDir);
+		if (next.command && next.cwd === void 0 && next.workingDirectory === void 0) next.cwd = employeeDir;
+		return [name, next];
+	}));
+}
+async function loadDigitalEmployeeWorkflows(employeeDir) {
+	const wfDir = path.join(employeeDir, "workflows");
+	if (!await pathExistsSafe(wfDir)) return "";
+	try {
+		const files = (await fs$1.readdir(wfDir)).filter((name) => name.endsWith(".md")).sort();
+		const parts = [];
+		for (const fileName of files) {
+			parts.push(await fs$1.readFile(path.join(wfDir, fileName), "utf-8"));
+		}
+		return parts.join("\n\n---\n\n");
+	} catch {
+		return "";
+	}
+}
+async function resolveDigitalEmployeeExecutionContext(cfg, opts) {
+	const requestedAgentId = normalizeOptionalString(opts?.executeAsAgentId);
+	if (!requestedAgentId) return null;
+	const agentId = normalizeAgentId(requestedAgentId);
+	const employeeDir = await resolveDigitalEmployeeDir(agentId);
+	if (!employeeDir) throw new Error(`Digital employee "${agentId}" is not installed`);
+	const employeeSkillsDir = path.join(employeeDir, "skills");
+	const employeeCfg = {
+		...cfg,
+		mcp: { ...cfg?.mcp, servers: await buildDigitalEmployeeMcpServers(employeeDir) },
+		skills: {
+			...cfg?.skills,
+			__digitalEmployeeOnly: true,
+			load: { ...cfg?.skills?.load, extraDirs: [employeeSkillsDir] }
+		}
+	};
+	const workflowText = await loadDigitalEmployeeWorkflows(employeeDir);
+	const isolationPrompt = [
+		"Digital employee isolation:",
+		"- Available skills and MCP servers are limited to this digital employee package.",
+		"- Ignore skills, MCP servers, or tool inventories mentioned earlier in the shared conversation unless they appear in the current package-local inventory.",
+		"- When asked about your skills, list only the package-local skills available to this digital employee."
+	].join("\n");
+	const workflowPrompt = [isolationPrompt, workflowText ? `<workflows>\n${workflowText}\n</workflows>` : ""].filter(Boolean).join("\n\n");
+	return { agentId, cfg: employeeCfg, workflowPrompt };
+}
 async function getReplyFromConfig(ctx, opts, configOverride) {
 	const isFastTestEnv = process.env.OPENCLAW_TEST_FAST === "1";
-	const cfg = resolveGetReplyConfig({
+	let cfg = resolveGetReplyConfig({
 		getRuntimeConfig,
 		isFastTestEnv,
 		configOverride
 	});
+	const digitalEmployeeExecution = await resolveDigitalEmployeeExecutionContext(cfg, opts);
+	if (digitalEmployeeExecution) cfg = digitalEmployeeExecution.cfg;
 	const resolverTiming = createReplyTimingTracker({
 		log: replyResolverTimingLog,
 		config: cfg
@@ -5066,7 +5151,7 @@ async function getReplyFromConfig(ctx, opts, configOverride) {
 		isFastTestEnv
 	}));
 	const finalized = resolverTiming.measureSync("reply.finalize_context", () => finalizeInboundContext(ctx));
-	const { agentSessionKey, agentId } = resolverTiming.measureSync("reply.resolve_agent_scope", () => {
+	const { agentSessionKey: sessionAgentKeyRaw, agentId: sessionAgentId } = resolverTiming.measureSync("reply.resolve_agent_scope", () => {
 		const resolvedAgentSessionKey = resolveCommandTurnTargetSessionKey(finalized) || finalized.SessionKey;
 		return {
 			agentSessionKey: resolvedAgentSessionKey,
@@ -5077,6 +5162,12 @@ async function getReplyFromConfig(ctx, opts, configOverride) {
 			})
 		};
 	});
+	const agentId = digitalEmployeeExecution?.agentId ?? sessionAgentId;
+	const agentSessionKey = sessionAgentKeyRaw;
+	const baseOpts = digitalEmployeeExecution ? {
+		...opts,
+		extraSystemPrompt: digitalEmployeeExecution.workflowPrompt || void 0
+	} : opts;
 	const traceAttributes = resolverTiming.measureSync("reply.resolve_trace_context", () => ({
 		surface: normalizeOptionalString(finalized.Surface ?? finalized.Provider) ?? "unknown",
 		hasSessionKey: Boolean(agentSessionKey),
@@ -5102,11 +5193,12 @@ async function getReplyFromConfig(ctx, opts, configOverride) {
 		config: cfg,
 		attributes: traceAttributes
 	}));
-	const mergedSkillFilter = resolverTiming.measureSync("reply.resolve_skill_filter", () => mergeSkillFilters(opts?.skillFilter, resolveAgentSkillsFilter(cfg, agentId)));
+	const oneTurnSkillFilter = Array.isArray(baseOpts?.skillFilter) ? baseOpts.skillFilter.map((entry) => typeof entry === "string" ? entry.trim() : "").filter(Boolean) : void 0;
+	const mergedSkillFilter = oneTurnSkillFilter && oneTurnSkillFilter.length > 0 ? oneTurnSkillFilter : resolverTiming.measureSync("reply.resolve_skill_filter", () => mergeSkillFilters(baseOpts?.skillFilter, resolveAgentSkillsFilter(cfg, agentId)));
 	const resolvedOpts = mergedSkillFilter !== void 0 ? {
-		...opts,
+		...baseOpts,
 		skillFilter: mergedSkillFilter
-	} : opts;
+	} : baseOpts;
 	const agentCfg = cfg.agents?.defaults;
 	const sessionCfg = cfg.session;
 	const { defaultProvider, defaultModel, aliasIndex } = resolverTiming.measureSync("reply.resolve_default_model", () => resolveDefaultModel({
@@ -5218,9 +5309,16 @@ async function getReplyFromConfig(ctx, opts, configOverride) {
 		cfg,
 		commandAuthorized
 	}));
-	const { sessionCtx, sessionEntry, previousSessionEntry, sessionStore, sessionKey, sessionId, isNewSession, resetTriggered, systemSent, storePath, sessionScope, groupResolution, isGroup, triggerBodyNormalized, bodyStripped } = sessionState;
-	let { abortedLastRun } = sessionState;
+	const { sessionCtx, previousSessionEntry, sessionStore, sessionKey, sessionId, isNewSession, resetTriggered, systemSent, storePath, sessionScope, groupResolution, isGroup, triggerBodyNormalized, bodyStripped } = sessionState;
+	let { sessionEntry, abortedLastRun } = sessionState;
 	resolverTimingSessionKey = sessionKey ?? resolverTimingSessionKey;
+	if (digitalEmployeeExecution) {
+		if (sessionEntry?.skillsSnapshot) {
+			sessionState.sessionEntry = { ...sessionEntry, skillsSnapshot: void 0 };
+			sessionEntry = sessionState.sessionEntry;
+		}
+		if (sessionStore && sessionKey) sessionStore[sessionKey] = { ...sessionStore[sessionKey], skillsSnapshot: void 0 };
+	}
 	if (sessionEntry?.pendingFinalDelivery && sessionEntry.pendingFinalDeliveryText) {
 		const text = sanitizePendingFinalDeliveryText(sessionEntry.pendingFinalDeliveryText);
 		if (opts?.isHeartbeat) {
diff --git a/dist/model-context-tokens-BdVmxFQp.js b/dist/model-context-tokens-BdVmxFQp.js
index 4abb3a3..f71588e 100644
--- a/dist/model-context-tokens-BdVmxFQp.js
+++ b/dist/model-context-tokens-BdVmxFQp.js
@@ -538,10 +538,14 @@ async function runAfterCompactionHooks(params) {
 //#endregion
 //#region src/agents/embedded-agent-runner/model-context-tokens.ts
 /** Returns finite context-token metadata when a model discovery source provided it. */
-/** Prefer contextTokens, then contextWindow, when present on model metadata. */
+/** Use the larger of contextTokens and contextWindow when both are present. */
 function readAgentModelContextTokens(model) {
-	const value = model?.contextTokens;
-	return typeof value === "number" && Number.isFinite(value) ? value : void 0;
+	const contextTokens = model?.contextTokens;
+	const contextWindow = model?.contextWindow;
+	const normalizedTokens = typeof contextTokens === "number" && Number.isFinite(contextTokens) ? contextTokens : void 0;
+	const normalizedWindow = typeof contextWindow === "number" && Number.isFinite(contextWindow) ? contextWindow : void 0;
+	if (normalizedTokens !== void 0 && normalizedWindow !== void 0) return Math.max(normalizedTokens, normalizedWindow);
+	return normalizedTokens ?? normalizedWindow;
 }
 //#endregion
 export { runAfterCompactionHooks as a, captureCompactionCheckpointSnapshotAsync as c, getSessionCompactionCheckpoint as d, listSessionCompactionCheckpoints as f, resolveSessionCompactionCheckpointReason as h, estimateTokensAfterCompaction as i, cleanupCompactionCheckpointSnapshot as l, readSessionLeafIdFromTranscriptAsync as m, asCompactionHookRunner as n, runBeforeCompactionHooks as o, persistSessionCompactionCheckpoint as p, buildBeforeCompactionHookMetrics as r, runPostCompactionSideEffects as s, readAgentModelContextTokens as t, forkCompactionCheckpointTranscriptAsync as u };
diff --git a/dist/openai-transport-stream-iDKSQw7Q.js b/dist/openai-transport-stream-iDKSQw7Q.js
index 4bc953a..3f1cd31 100644
--- a/dist/openai-transport-stream-iDKSQw7Q.js
+++ b/dist/openai-transport-stream-iDKSQw7Q.js
@@ -3653,9 +3653,10 @@ function estimateOpenAICompletionsContentChars(value) {
 	return adjustedChars;
 }
 function resolveOpenAICompletionsEffectiveContextTokens(model) {
-	const contextTokens = model.contextTokens;
-	if (typeof contextTokens === "number" && Number.isFinite(contextTokens) && contextTokens > 0) return contextTokens;
-	return typeof model.contextWindow === "number" && Number.isFinite(model.contextWindow) && model.contextWindow > 0 ? model.contextWindow : void 0;
+	const contextTokens = typeof model.contextTokens === "number" && Number.isFinite(model.contextTokens) && model.contextTokens > 0 ? model.contextTokens : void 0;
+	const contextWindow = typeof model.contextWindow === "number" && Number.isFinite(model.contextWindow) && model.contextWindow > 0 ? model.contextWindow : void 0;
+	if (contextTokens !== void 0 && contextWindow !== void 0) return Math.max(contextTokens, contextWindow);
+	return contextTokens ?? contextWindow;
 }
 function isQwenOpenAICompletionsThinkingFormat(format) {
 	return format === "qwen" || format === "qwen-chat-template";
diff --git a/dist/schema-KBhSSPA3.js b/dist/schema-KBhSSPA3.js
index 8fabdd4..d26472f 100644
--- a/dist/schema-KBhSSPA3.js
+++ b/dist/schema-KBhSSPA3.js
@@ -2745,8 +2745,11 @@ const ChatSendParamsSchema = Type.Object({
 	systemInputProvenance: Type.Optional(InputProvenanceSchema),
 	systemProvenanceReceipt: Type.Optional(Type.String()),
 	suppressCommandInterpretation: Type.Optional(Type.Boolean()),
+	extraSystemPrompt: Type.Optional(Type.String()),
+	executeAsAgentId: Type.Optional(Type.String()),
+	executedByAgentName: Type.Optional(Type.String()),
 	idempotencyKey: NonEmptyString
-}, { additionalProperties: false });
+}, { additionalProperties: true });
 /** Cancels the active or named run for a chat session. */
 const ChatAbortParamsSchema = Type.Object({
 	sessionKey: NonEmptyString,
diff --git a/dist/workspace-P8p68RCT.js b/dist/workspace-P8p68RCT.js
index be7afdd..098700b 100644
--- a/dist/workspace-P8p68RCT.js
+++ b/dist/workspace-P8p68RCT.js
@@ -865,7 +865,7 @@ function loadSkillEntries(workspaceDir, opts) {
 	const bundledSkillsDir = workspaceOnly ? void 0 : opts?.bundledSkillsDir ?? resolveBundledSkillsDir();
 	const pluginSkillsDir = opts?.pluginSkillsDir ?? path.join(CONFIG_DIR, "plugin-skills");
 	const extraDirs = normalizeTrimmedStringList(workspaceOnly ? [] : opts?.config?.skills?.load?.extraDirs ?? []);
-	const pluginSkillDirs = workspaceOnly ? [] : resolvePluginSkillDirs({
+	const pluginSkillDirs = (workspaceOnly || opts?.config?.skills?.__digitalEmployeeOnly) ? [] : resolvePluginSkillDirs({
 		workspaceDir,
 		config: opts?.config,
 		pluginSkillsDir
@@ -907,6 +907,34 @@ function loadSkillEntries(workspaceDir, opts) {
 	});
 	const merged = /* @__PURE__ */ new Map();
 	for (const record of extraSkills) merged.set(record.skill.name, record);
+	if (opts?.config?.skills?.__digitalEmployeeOnly === true) {
+		return Array.from(merged.values()).toSorted((a, b) => a.skill.name.localeCompare(b.skill.name, "en")).map((record) => {
+		const skill = record.skill;
+		const frontmatter = record.frontmatter ?? readSkillFrontmatterSafe({
+			rootDir: skill.baseDir,
+			filePath: skill.filePath,
+			maxBytes: limits.maxSkillFileBytes
+		}) ?? {};
+		const invocation = resolveSkillInvocationPolicy(frontmatter);
+		const entry = {
+			skill,
+			frontmatter,
+			metadata: resolveSkillEntryMetadata({
+				frontmatter,
+				skillDir: skill.baseDir
+			}),
+			invocation,
+			exposure: {
+				includeInRuntimeRegistry: true,
+				includeInAvailableSkillsPrompt: !invocation.disableModelInvocation,
+				userInvocable: invocation.userInvocable ?? true
+			}
+		};
+		if (record.syncSourceDir !== void 0) entry.syncSourceDir = record.syncSourceDir;
+		if (record.syncDirName !== void 0) entry.syncDirName = record.syncDirName;
+		return entry;
+	});
+	}
 	for (const record of bundledSkills) merged.set(record.skill.name, record);
 	for (const record of managedSkills) merged.set(record.skill.name, record);
 	for (const record of personalAgentsSkills) merged.set(record.skill.name, record);
diff --git a/dist/service-BqOmTMr8.js b/dist/service-BqOmTMr8.js
--- a/dist/service-BqOmTMr8.js
+++ b/dist/service-BqOmTMr8.js
@@ -211,9 +211,9 @@ function resolveSkillProposalTarget(params) {
 	const skillKey = normalizeSkillIndexName(params.skillName);
 	if (!skillKey) throw new Error("Skill name must contain at least one letter or number.");
-	const skillDir = path.resolve(params.workspaceDir, "skills", skillKey);
+	const skillDir = path.resolve(resolveManagedSkillsDir(), skillKey);
 	const skillFile = path.join(skillDir, "SKILL.md");
-	assertInsideWorkspace(params.workspaceDir, skillDir, "skill directory");
-	assertInsideWorkspace(params.workspaceDir, skillFile, "skill file");
+	assertInsideSkillWorkshopTarget(params.workspaceDir, skillDir, "skill directory");
+	assertInsideSkillWorkshopTarget(params.workspaceDir, skillFile, "skill file");
 	return {
 		skillKey,
 		skillDir,
@@ -386,9 +386,9 @@ async function readWorkspaceSupportFile(params) {
 	})).buffer.toString("utf8");
 }
 async function writeWorkspaceSkillFile(params) {
-	assertInsideWorkspace(params.workspaceDir, params.filePath, "skill file");
-	const relativePath = path.relative(path.resolve(params.workspaceDir), path.resolve(params.filePath));
-	await (await root(params.workspaceDir)).write(relativePath, params.content, {
+	const targetRoot = resolveSkillWorkshopTargetRoot(params.workspaceDir, params.filePath);
+	const relativePath = path.relative(targetRoot, path.resolve(params.filePath));
+	await (await root(targetRoot)).write(relativePath, params.content, {
 		encoding: "utf8",
 		mkdir: true,
 		...params.overwrite === void 0 ? {} : { overwrite: params.overwrite }
@@ -428,6 +428,24 @@ function assertInsideWorkspace(workspaceDir, targetPath, label) {
 	const resolvedTarget = path.resolve(targetPath);
 	if (resolvedTarget !== resolvedWorkspaceDir && !isPathInside(resolvedWorkspaceDir, resolvedTarget)) throw new Error(`${label} must stay inside the workspace.`);
 }
+function resolveManagedSkillsDir() {
+	return path.resolve(resolveStateDir(), "skills");
+}
+function resolveSkillWorkshopTargetRoot(workspaceDir, targetPath) {
+	const resolvedTarget = path.resolve(targetPath);
+	const resolvedWorkspaceDir = path.resolve(workspaceDir);
+	if (resolvedTarget === resolvedWorkspaceDir || isPathInside(resolvedWorkspaceDir, resolvedTarget)) return resolvedWorkspaceDir;
+	const managedSkillsDir = resolveManagedSkillsDir();
+	if (resolvedTarget === managedSkillsDir || isPathInside(managedSkillsDir, resolvedTarget)) return managedSkillsDir;
+	throw new Error("Skill Workshop target must stay inside the workspace or managed skills directory.");
+}
+function assertInsideSkillWorkshopTarget(workspaceDir, targetPath, label) {
+	try {
+		resolveSkillWorkshopTargetRoot(workspaceDir, targetPath);
+	} catch {
+		throw new Error(`${label} must stay inside the workspace or managed skills directory.`);
+	}
+}
 function assertProposalId(proposalId) {
 	if (!PROPOSAL_ID_PATTERN.test(proposalId)) throw new Error("Invalid skill proposal id.");
 }
@@ -509,7 +527,7 @@ function proposalRelativeDir(proposalId) {
 }
 //#endregion
 //#region src/skills/workshop/service.ts
-const WRITABLE_WORKSPACE_SOURCES = new Set(["openclaw-workspace", "agents-skills-project"]);
+const WRITABLE_WORKSPACE_SOURCES = new Set(["openclaw-managed"]);
 const MAX_PROPOSAL_DRAFT_BYTES = 1024 * 1024;
 const MAX_PROPOSAL_DIRECTORY_ENTRIES = 256;
 const MAX_SKILL_PROPOSAL_DESCRIPTION_BYTES = 160;
@@ -655,7 +673,7 @@ async function proposeCreateSkill(input) {
 			skillKey: target.skillKey,
 			skillDir: target.skillDir,
 			skillFile: target.skillFile,
-			source: "openclaw-workspace"
+			source: "openclaw-managed"
 		},
 		scan: scanProposalBundle(proposalContent, supportFiles),
 		...supportFiles.length > 0 ? { supportFiles: await buildSupportFileMetadata(supportFiles) } : {},
@@ -745,8 +763,8 @@ async function reviseSkillProposal(input) {
 	const config = resolveSkillWorkshopConfig(input.config);
 	return await withPendingSkillProposalMutation(input, "revised", async (read) => {
 		const { record } = read;
-		assertInsideWorkspace(input.workspaceDir, record.target.skillFile, "skill file");
-		assertInsideWorkspace(input.workspaceDir, record.target.skillDir, "skill directory");
+		assertInsideSkillWorkshopTarget(input.workspaceDir, record.target.skillFile, "skill file");
+		assertInsideSkillWorkshopTarget(input.workspaceDir, record.target.skillDir, "skill directory");
 		if (record.kind === "create") {
 			if (await readWorkspaceSkillFile(record.target.skillFile) !== null) {
 				await markProposalStale(record, "Target skill was created after proposal creation.");
@@ -847,8 +865,8 @@ async function applySkillProposal(input) {
 			} });
 			throw new Error("Proposal scan failed; proposal was quarantined.");
 		}
-		assertInsideWorkspace(input.workspaceDir, record.target.skillFile, "skill file");
-		assertInsideWorkspace(input.workspaceDir, record.target.skillDir, "skill directory");
+		assertInsideSkillWorkshopTarget(input.workspaceDir, record.target.skillFile, "skill file");
+		assertInsideSkillWorkshopTarget(input.workspaceDir, record.target.skillDir, "skill directory");
 		const targetState = await readApplyTargetState(record, supportFiles);
 		const rollback = createSkillProposalRollback({
 			proposalId: record.id,
@@ -1142,8 +1160,8 @@ async function hydrateProposalSupportFiles(read) {
 }
 function isProposalInWorkspace(record, workspaceDir) {
 	try {
-		assertInsideWorkspace(workspaceDir, record.target.skillFile, "skill file");
-		assertInsideWorkspace(workspaceDir, record.target.skillDir, "skill directory");
+		assertInsideSkillWorkshopTarget(workspaceDir, record.target.skillFile, "skill file");
+		assertInsideSkillWorkshopTarget(workspaceDir, record.target.skillDir, "skill directory");
 		return true;
 	} catch {
 		return false;
@@ -1175,8 +1193,8 @@ function proposalMatchesName(proposal, name) {
 }
 function assertWritableSkillTarget(workspaceDir, skill) {
 	if (!WRITABLE_WORKSPACE_SOURCES.has(skill.source)) throw new Error(`Skill source is not writable by Skill Workshop: ${skill.source}`);
-	assertInsideWorkspace(workspaceDir, skill.filePath, "skill file");
-	assertInsideWorkspace(workspaceDir, skill.baseDir, "skill directory");
+	assertInsideSkillWorkshopTarget(workspaceDir, skill.filePath, "skill file");
+	assertInsideSkillWorkshopTarget(workspaceDir, skill.baseDir, "skill directory");
 	if (path.basename(skill.filePath) !== "SKILL.md") throw new Error("Skill Workshop can only update SKILL.md targets.");
 }
 function normalizeRequired(value, label) {
diff --git a/dist/openclaw-tools-YEJwiczM.js b/dist/openclaw-tools-YEJwiczM.js
--- a/dist/openclaw-tools-YEJwiczM.js
+++ b/dist/openclaw-tools-YEJwiczM.js
@@ -12330,6 +12330,6 @@ function createSkillWorkshopTool(options) {
 				});
 				return actionResult(applied.record, {
-					contentText: `Applied skill proposal ${applied.record.id}.`,
+					contentText: `Applied skill proposal ${applied.record.id} to ${applied.targetSkillFile}.`,
 					targetSkillFile: applied.targetSkillFile
 				});
 			}
