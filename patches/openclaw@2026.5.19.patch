--- a/dist/subagent-spawn-D7Cv1da6.js
+++ b/dist/subagent-spawn-D7Cv1da6.js
@@ -32,6 +32,11 @@
 import { n as resolveParentForkDecision, t as forkSessionFromParent } from "./session-fork-DmANnKcu.js";
 import { t as ensureContextEnginesInitialized } from "./init-NHJ_X2Me.js";
 import { t as buildSubagentSystemPrompt } from "./subagent-system-prompt-D82gTl1J.js";
+import { existsSync, readFileSync, readdirSync, appendFileSync } from "fs";
+import { join as joinPath } from "path";
+import { homedir as userHomeDir } from "os";
+import { runEmbeddedPiAgent } from "./pi-embedded.runtime-Dukha8NF.js";
+import { o as resolveSessionTranscriptPath } from "./paths-RbVDOqFp.js";
+import { t as buildWorkspaceSkillSnapshot } from "./workspace-DpPIWO-c.js";
 import { promises } from "node:fs";
 import path from "node:path";
 import crypto from "node:crypto";
@@ -629,6 +634,25 @@
 function hasRoutableDeliveryOrigin(origin) {
 	return Boolean(origin?.channel && origin.to);
 }
+function resolveEmployeeDir(agentId) {
+	const d = joinPath(userHomeDir(), ".openclaw", "digital-employee", agentId);
+	return existsSync(d) ? d : null;
+}
+function buildEmployeeMcpConfig(employeeDir) {
+	const p = joinPath(employeeDir, "mcp", "servers.json");
+	if (!existsSync(p)) return {};
+	try { return JSON.parse(readFileSync(p, "utf-8")).servers ?? {}; }
+	catch { return {}; }
+}
+function loadEmployeeWorkflows(employeeDir) {
+	const wfDir = joinPath(employeeDir, "workflows");
+	if (!existsSync(wfDir)) return "";
+	try {
+		const files = readdirSync(wfDir).filter(f => f.endsWith(".md"));
+		if (files.length === 0) return "";
+		return files.map(f => readFileSync(joinPath(wfDir, f), "utf-8")).join("\n\n---\n\n");
+	} catch { return ""; }
+}
 async function spawnSubagentDirect(params, ctx) {
 	const task = params.task;
 	const taskNameResult = normalizeSubagentTaskName(params.taskName);
@@ -947,6 +971,50 @@
 		persistentSession: spawnMode === "session",
 		task
 	});
+	const employeeDir = resolveEmployeeDir(targetAgentId);
+	if (employeeDir) {
+		const employeeSkillsDir = joinPath(employeeDir, "skills");
+		const employeeCfg = {
+			...cfg,
+			mcp: { ...cfg?.mcp, servers: buildEmployeeMcpConfig(employeeDir) },
+			skills: {
+				...cfg?.skills,
+				__digitalEmployeeOnly: true,
+				load: { ...cfg?.skills?.load, extraDirs: [employeeSkillsDir] }
+			}
+		};
+		const employeeWorkflows = loadEmployeeWorkflows(employeeDir);
+		const taskMessage = employeeWorkflows
+			? "<workflows>\n" + employeeWorkflows + "\n</workflows>\n\n---\n\n" + childTaskMessage
+			: childTaskMessage;
+		const employeeSkillsSnapshot = buildWorkspaceSkillSnapshot(spawnedWorkspaceDir, {
+			config: employeeCfg,
+			agentId: targetAgentId
+		});
+		try {
+			appendFileSync(joinPath(userHomeDir(), ".openclaw", "de-run.log"), `${new Date().toISOString()} employeeSkills=${employeeSkillsSnapshot.resolvedSkills?.map(s => s.name).join(",") || "(none)"}\n`);
+		} catch {}
+		try {
+			const runResult = await runEmbeddedPiAgent({
+				config: employeeCfg,
+				sessionKey: requesterInternalKey,
+				sessionId: requesterSessionKey,
+				sessionFile: resolveSessionTranscriptPath(requesterSessionKey?.includes(":") ? requesterSessionKey.split(":").pop() : requesterSessionKey, requesterAgentId),
+				runId: childIdem,
+				agentId: targetAgentId,
+				workspaceDir: spawnedWorkspaceDir,
+				agentDir: resolveAgentDir(cfg, targetAgentId),
+				prompt: taskMessage,
+				extraSystemPrompt: childSystemPrompt,
+				skillsSnapshot: employeeSkillsSnapshot,
+				thinking: thinkingOverride,
+				timeoutMs: (runTimeoutSeconds ?? 900) * 1000,
+				lane: AGENT_LANE_SUBAGENT,
+				trigger: "manual",
+				senderIsOwner: true,
+				allowGatewaySubagentBinding: false,
+				cleanupBundleMcpOnRunEnd: true
+			});
+			const outputText = runResult.outputText ?? runResult.summary ?? "";
+			return {
+				status: "completed",
+				childSessionKey: requesterSessionKey,
+				runId: childIdem,
+				mode: "run",
+				output: outputText,
+				meta: runResult.meta
+			};
+		} catch (err) {
+			return {
+				status: "error",
+				error: `Employee '${targetAgentId}' inline run failed: ${err?.message ?? err}`,
+				childSessionKey: requesterSessionKey
+			};
+		}
+	}
 	const spawnedMetadata = normalizeSpawnedRunMetadata({
 		spawnedBy: spawnedByKey,
 		...toolSpawnMetadata,

--- a/dist/chat-O8o_e6f5.js
+++ b/dist/chat-O8o_e6f5.js
@@ -2217,6 +2217,7 @@
 					modelOverrideFallbacks,
 					thinkingLevelOverride: p.thinking,
 					fastModeOverride: p.fastMode,
+					extraSystemPrompt: p.extraSystemPrompt,
 					onAgentRunStart: (runId) => {
 						agentRunStarted = true;
 						if (!hasBeforeAgentRunGate) emitUserTranscriptUpdate();

--- a/dist/openclaw-tools-DJxO67FO.js
+++ b/dist/openclaw-tools-DJxO67FO.js
@@ -9161,7 +9161,7 @@
 				}
 				return jsonResult(addRoleToFailureResult(result, requestedAgentId));
 			}
-			return jsonResult(addRoleToFailureResult(await spawnSubagentDirect({
+			const spawnResult = await spawnSubagentDirect({
 				task,
 				taskName,
 				label: label || void 0,
@@ -9194,7 +9194,14 @@
 				workspaceDir: opts?.workspaceDir,
 				inheritedToolAllowlist: opts?.inheritedToolAllowlist,
 				inheritedToolDenylist: opts?.inheritedToolDenylist
-			}), requestedAgentId));
+			});
+			if (spawnResult.status === "completed") {
+				return {
+					content: [{ type: "text", text: spawnResult.output || "Task completed." }],
+					meta: { childSessionKey: spawnResult.childSessionKey, runId: spawnResult.runId, inline: true }
+				};
+			}
+			return jsonResult(addRoleToFailureResult(spawnResult, requestedAgentId));
 		}
 	};
 }

--- a/dist/protocol-CjAsjR7U.js
+++ b/dist/protocol-CjAsjR7U.js
@@ -2031,7 +2031,8 @@
 	timeoutMs: Type.Optional(Type.Integer({ minimum: 0 })),
 	systemInputProvenance: Type.Optional(InputProvenanceSchema),
 	systemProvenanceReceipt: Type.Optional(Type.String()),
-	idempotencyKey: NonEmptyString
+	idempotencyKey: NonEmptyString,
+		extraSystemPrompt: Type.Optional(Type.String())
 }, { additionalProperties: false });
 const ChatAbortParamsSchema = Type.Object({
 	sessionKey: NonEmptyString,

--- a/dist/get-reply-DbVszRXg.js
+++ b/dist/get-reply-DbVszRXg.js
@@ -2598,6 +2598,7 @@
 	}).allowEmptyAssistantReplyAsSilent;
 	const groupSystemPrompt = normalizeOptionalString(promptSessionCtx.GroupSystemPrompt) ?? "";
 	const extraSystemPromptParts = [
+		opts?.extraSystemPrompt,
 		buildInboundMetaSystemPrompt(isNewSession ? sessionCtx : {
 			...sessionCtx,
 			ThreadStarterBody: void 0

--- a/dist/workspace-DpPIWO-c.js
+++ b/dist/workspace-DpPIWO-c.js
@@ -714,6 +714,31 @@
 		source: "openclaw-extra",
 		limits
 	})];
+	const mapSkillRecords = (records) => records.sort((a, b) => a.skill.name.localeCompare(b.skill.name, "en")).map((record) => {
+		const skill = record.skill;
+		const frontmatter = record.frontmatter ?? readSkillFrontmatterSafe({
+			rootDir: skill.baseDir,
+			filePath: skill.filePath,
+			maxBytes: limits.maxSkillFileBytes
+		}) ?? {};
+		const invocation = resolveSkillInvocationPolicy(frontmatter);
+		return {
+			skill,
+			frontmatter,
+			metadata: resolveOpenClawMetadata(frontmatter),
+			invocation,
+			...record.syncSourceDir !== void 0 ? { syncSourceDir: record.syncSourceDir } : {},
+			...record.syncDirName !== void 0 ? { syncDirName: record.syncDirName } : {},
+			exposure: {
+				includeInRuntimeRegistry: true,
+				includeInAvailableSkillsPrompt: invocation.disableModelInvocation !== true,
+				userInvocable: invocation.userInvocable !== false
+			}
+		};
+	});
+	if (opts?.config?.skills?.__digitalEmployeeOnly === true) {
+		const mergedEmployeeSkills = /* @__PURE__ */ new Map();
+		for (const record of extraSkills) mergedEmployeeSkills.set(record.skill.name, record);
+		return mapSkillRecords(Array.from(mergedEmployeeSkills.values()));
+	}
 	const managedSkills = loadSkills({
 		dir: managedSkillsDir,
 		source: "openclaw-managed"
@@ -738,27 +763,7 @@
 	for (const record of personalAgentsSkills) merged.set(record.skill.name, record);
 	for (const record of projectAgentsSkills) merged.set(record.skill.name, record);
 	for (const record of workspaceSkills) merged.set(record.skill.name, record);
-	return Array.from(merged.values()).sort((a, b) => a.skill.name.localeCompare(b.skill.name, "en")).map((record) => {
-		const skill = record.skill;
-		const frontmatter = record.frontmatter ?? readSkillFrontmatterSafe({
-			rootDir: skill.baseDir,
-			filePath: skill.filePath,
-			maxBytes: limits.maxSkillFileBytes
-		}) ?? {};
-		const invocation = resolveSkillInvocationPolicy(frontmatter);
-		return {
-			skill,
-			frontmatter,
-			metadata: resolveOpenClawMetadata(frontmatter),
-			invocation,
-			...record.syncSourceDir !== void 0 ? { syncSourceDir: record.syncSourceDir } : {},
-			...record.syncDirName !== void 0 ? { syncDirName: record.syncDirName } : {},
-			exposure: {
-				includeInRuntimeRegistry: true,
-				includeInAvailableSkillsPrompt: invocation.disableModelInvocation !== true,
-				userInvocable: invocation.userInvocable !== false
-			}
-		};
-	});
+	return mapSkillRecords(Array.from(merged.values()));
 }
 function escapeXml(str) {
 	return str.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&apos;");

--- a/dist/dangerous-tools-BCJ2wq8F.js
+++ b/dist/dangerous-tools-BCJ2wq8F.js
@@ -12,7 +12,6 @@
 	"fs_delete",
 	"fs_move",
 	"apply_patch",
-	"sessions_spawn",
 	"sessions_send",
 	"cron",
 	"gateway",
